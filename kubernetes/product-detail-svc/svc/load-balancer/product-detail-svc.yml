apiVersion: v1
kind: Service
metadata:
  name: product-detail-load-balancer-svc
  labels:
    name: product-detail-load-balancer-svc
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: product-detail-deploy
  type: LoadBalancer
