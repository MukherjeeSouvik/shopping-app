apiVersion: v1
kind: Service
metadata:
  name: product-frontend-load-balancer-svc
  labels:
    name: product-frontend-load-balancer-svc
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: product-frontend-deploy
  type: LoadBalancer
