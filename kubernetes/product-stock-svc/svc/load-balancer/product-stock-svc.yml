apiVersion: v1
kind: Service
metadata:
  name: product-stock-load-balancer-svc
  labels:
    name: product-stock-load-balancer-svc
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: product-stock-deploy
  type: LoadBalancer
