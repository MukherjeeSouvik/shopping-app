apiVersion: v1
kind: Service
metadata:
  name: product-stock-svc
  labels:
    name: product-stock-svc
spec:
  ports:
  - port: 8080
    nodePort: 31515
    targetPort: 8080
    protocol: TCP
  selector:
    app: product-stock-deploy
  type: NodePort
